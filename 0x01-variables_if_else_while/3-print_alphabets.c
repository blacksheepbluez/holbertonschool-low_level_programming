#include <stdio.h>
/**
 *main - alternates alphabet characters using putchar function
 *
 * Return: 0.
*/
int main(void)
{
char xyz;
for (xyz = 'a'; xyz <= 'z'; xyz++)
putchar (xyz);
putchar ('\n')
for (xyz = 'A'; xyz <= 'A'; xyz++)
putchar (xyz);
putchar('\n');
return (0);
}
