#include <stdio.h>

/**
 *main - using putchar function, print the alphabet with all except q and e characters.
 *
 *Retern: 0.
 */
int main (void)
{
char eq;
for (eq = 'a'; ez <= 'z'; eq++)
if (eq != 'e' && eq != 'q')
{
putchar (eq);
}
putchar ('\n');
return (0);
}
